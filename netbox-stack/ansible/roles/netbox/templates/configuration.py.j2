# =============================================================================
# NetBox Configuration - Generated by Ansible
# =============================================================================

ALLOWED_HOSTS = ['*']

DATABASE = {
    'NAME': '{{ netbox_db_name }}',
    'USER': '{{ netbox_db_user }}',
    'PASSWORD': '{{ netbox_db_password }}',
    'HOST': '{{ netbox_db_host }}',
    'PORT': '{{ netbox_db_port }}',
    'CONN_MAX_AGE': 300,
}

REDIS = {
    'tasks': {
        'HOST': '{{ netbox_redis_host }}',
        'PORT': {{ netbox_redis_port }},
        'PASSWORD': '',
        'DATABASE': 0,
        'SSL': False,
    },
    'caching': {
        'HOST': '{{ netbox_redis_host }}',
        'PORT': {{ netbox_redis_port }},
        'PASSWORD': '',
        'DATABASE': 1,
        'SSL': False,
    }
}

SECRET_KEY = '{{ netbox_secret_key }}'

# CSRF - inclure toutes les origines (WAN NAT + interne)
CSRF_TRUSTED_ORIGINS = [
    'http://10.0.20.30',
    'http://10.0.20.30:80',
    'http://192.168.80.141:8095',
    'http://192.168.80.141',
    'http://netbox-01.infra.lan',
    'http://netbox.infra.lan',
    'https://netbox-01.infra.lan',
    'https://netbox.infra.lan',
]

# Reglages
DEBUG = False
LOGIN_REQUIRED = True
TIME_ZONE = 'Europe/Paris'
DATE_FORMAT = 'd/m/Y'
SHORT_DATE_FORMAT = 'd/m/Y'
TIME_FORMAT = 'H:i:s'
DATETIME_FORMAT = 'd/m/Y H:i'

# Email
EMAIL = {
    'SERVER': 'localhost',
    'PORT': 25,
    'USERNAME': '',
    'PASSWORD': '',
    'USE_SSL': False,
    'USE_TLS': False,
    'TIMEOUT': 10,
    'FROM_EMAIL': 'netbox@infra.lan',
}

# Reverse proxy
USE_X_FORWARDED_HOST = True
SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
